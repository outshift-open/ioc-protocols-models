# LSTP Python Bindings

Status: Reserved — not yet implemented.

Python bindings for LSTP will follow the same structure as the [SSTP Python bindings](../../SSTP/language-bindings/Python/README.md) once the sub-protocol is defined.
