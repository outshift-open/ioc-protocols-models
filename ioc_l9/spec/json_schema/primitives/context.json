{
  "$defs": {
    "Epistemic": {
      "description": "Participant epistemic (belief/knowledge) state at the time the message was sent.\nCurrently a placeholder \u2014 fields will be added as the model is defined.",
      "properties": {},
      "title": "Epistemic",
      "type": "object"
    },
    "Provenance": {
      "description": "Tracks the origin and lineage of a message \u2014 who created it, from what source,\nand through which transformations. Fields TBD.",
      "properties": {},
      "title": "Provenance",
      "type": "object"
    },
    "Semantic": {
      "description": "Describes the semantic/ontological framework needed to correctly interpret the payload.\nImplementations use this to select the appropriate handler for the message.",
      "properties": {
        "schema_id": {
          "title": "Schema Id",
          "type": "string"
        },
        "ontology_ref": {
          "title": "Ontology Ref",
          "type": "string"
        },
        "provenance": {
          "anyOf": [
            {
              "$ref": "#/$defs/Provenance"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "schema_id",
        "ontology_ref"
      ],
      "title": "Semantic",
      "type": "object"
    }
  },
  "properties": {
    "topic": {
      "title": "Topic",
      "type": "string"
    },
    "epistemic": {
      "anyOf": [
        {
          "$ref": "#/$defs/Epistemic"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "semantic": {
      "anyOf": [
        {
          "$ref": "#/$defs/Semantic"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    }
  },
  "required": [
    "topic"
  ],
  "title": "Context",
  "type": "object",
  "version": "0.0.3"
}